spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dbdemo?useSSL=true
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
